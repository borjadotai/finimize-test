import { extendTheme } from '@chakra-ui/react'

const config = {
    initialColorMode: 'light',
    useSystemColorMode: false,
}

const themeConfing = extendTheme({ config })

export default themeConfing
